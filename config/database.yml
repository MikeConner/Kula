# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  username: rails
  password: rails
  host: kulatest.cjc0nanez7zy.us-east-1.rds.amazonaws.com
  encoding: utf8
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: kula_dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: kula_test

production:
  <<: *default
  database: kula_prod
